# 微信推送问题排查指南

## 重要说明

**真机调试环境是可以收到微信推送的，不需要上线小程序。**

## 推送不工作的常见原因

### 1. 用户未订阅消息模板（最常见）

微信订阅消息**必须**先由用户主动订阅才能收到推送。订阅操作发生在：
- 绑定设备后
- 创建围栏后

**检查方法：**
- 在小程序中，当您绑定设备或创建围栏时，应该会弹出订阅授权弹窗
- 您必须点击"允许"或"总是"才能成功订阅
- 如果点击了"拒绝"，将无法收到推送

### 2. 配置问题

**检查项：**
- `WECHAT_APPID` 和 `WECHAT_SECRET` 是否正确配置在 `.env` 文件中
- 模板ID是否已在微信公众平台配置

### 3. 订阅状态丢失

如果之前订阅过，但现在收不到推送，可能是订阅状态丢失。需要重新订阅。

## 调试步骤

### 步骤1：检查订阅状态

可以通过以下方式检查订阅状态：

1. **查看订阅记录**：调用 `/auth/subscribe/` 接口查看已订阅的模板
2. **查看模板ID配置**：调用 `/auth/subscribe/template_ids/` 接口查看配置的模板ID
3. **查看后端日志**：检查日志文件中的推送相关记录

### 步骤2：手动触发订阅

如果未订阅，需要手动触发订阅：

**方法1：在小程序中**
1. 进入"老人详情"页面
2. 绑定一个设备（如果还未绑定）
3. 会弹出订阅授权弹窗，点击"允许"

**方法2：创建围栏**
1. 进入"电子围栏"页面
2. 创建一个围栏
3. 会弹出订阅授权弹窗，点击"允许"

### 步骤3：检查推送日志

查看后端日志文件：

```bash
cd /root/Project/backend
tail -f logs/django.log | grep -i "推送\|subscribe\|access_token"
```

关键日志信息：
- `成功获取微信access_token` - 配置正确
- `订阅消息发送成功` - 推送成功
- `errcode=43101` - 用户未订阅
- `errcode=43104` - 用户拒绝接收
- `获取access_token失败` - 配置错误

### 步骤4：测试推送

触发一个警报来测试推送：
- 发送SOS求救
- 触发围栏越界
- 设备离线（需要等待超时）
- 低电量警告

## 常见错误码

| 错误码 | 说明 | 解决方法 |
|--------|------|----------|
| 43101 | 用户未订阅 | 需要重新订阅消息模板 |
| 43104 | 用户拒绝接收 | 需要在设置中重新授权 |
| 40001 | access_token无效 | 自动重试，通常会自动恢复 |
| 40014 | access_token无效 | 自动重试，通常会自动恢复 |
| 42001 | access_token过期 | 自动重试，通常会自动恢复 |
| 45011 | 频率限制 | 等待一段时间后重试 |

## 订阅消息的限制

1. **订阅时效**：订阅授权有时效性，过期后需要重新订阅
2. **用户操作**：必须由用户主动触发订阅（不能后台自动订阅）
3. **频率限制**：同一个模板，同一用户每天最多收到一次推送（某些情况下）
4. **模板限制**：必须在微信公众平台配置了模板才能使用

## 快速检查清单

- [ ] `.env` 文件中配置了 `WECHAT_APPID` 和 `WECHAT_SECRET`
- [ ] 用户已在微信小程序中订阅了消息模板（点击了"允许"）
- [ ] 用户的openid存在（登录时获取）
- [ ] 设备已正确关联到监护人或老人
- [ ] 模板ID已在微信公众平台配置
- [ ] 后端日志中access_token获取成功

